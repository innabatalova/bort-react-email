import { IFontConfig } from "./IFontConfig"

export const fontConfig: IFontConfig = {
  'primary': ['16px', {
    lineHeight: '26px',
    fontWeight: '600'
  }],
  'secondary': ['14px', {
    lineHeight: '21px',
    fontWeight: '600'
  }]
}